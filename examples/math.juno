// examples/math.juno - Math library for Juno

fn square(x) {
    return x * x
}

fn cube(x) {
    return x * x * x
}

fn double(x) {
    return x * 2
}

fn factorial(n) {
    if (n <= 1) {
        return 1
    }
    return n * factorial(n - 1)
}

fn worker(arg) {
}

fn main() {
    prints("Main starting thread")
    let stack = alloc_stack(131072)
    thread_create(worker, stack, 42)
    sleep(100)
    prints("Main exiting")
    return 0
}

main()

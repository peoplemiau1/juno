fn main() {
    let p1 = malloc(16)
    let p2 = p1          // p1 is moved to p2
    
    // print(*p1)           // Error: use of moved value
    
    free(p2)
    free(p2)             // Error: double free
    return 0
}

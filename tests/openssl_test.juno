extern fn OpenSSL_version(t: i32) -> ptr from "libcrypto.so.3"
extern fn puts(s: ptr) -> i32 from "libc.so.6"

fn main() -> i64 {
    output("Starting...")
    output("OpenSSL Version Info:")

    let v = OpenSSL_version(0)
    output(v)

    let p = OpenSSL_version(3)
    output(p)

    return 0
}

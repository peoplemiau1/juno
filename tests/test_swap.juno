// Test swap function with pointers

fn swap(a, b) {
    let tmp = *a
    *a = *b
    *b = tmp
}

fn main() {
    let x = 100
    let y = 200
    swap(&x, &y)
    // After swap: x=200, y=100
    return x + y
}

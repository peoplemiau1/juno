// Juno v1.0 Test Suite

fn main() {
    output("=== JUNO v1.0 TEST ===")
    
    // 1. Math with all operators
    let a = 20
    let b = 4
    let sum = a + b
    let diff = a - b
    let prod = a * b
    let quot = a / b
    
    output("20 + 4 =")
    output_int(sum)
    output("20 - 4 =")
    output_int(diff)
    output("20 * 4 =")
    output_int(prod)
    output("20 / 4 =")
    output_int(quot)
    
    // 2. While loop
    output("While loop 3 to 1:")
    let count = 3
    while (count != 0) {
        output_int(count)
        count--
    }
    
    // 3. For loop
    output("For loop 1 to 3:")
    for (i = 1; i < 4; i++) {
        output_int(i)
    }
    
    output("=== ALL TESTS PASSED ===")
    return 0
}

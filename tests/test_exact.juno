// Exact same as test_call_order but with add name

fn add(a, b) {
    return a + b
}

fn main() {
    let sum = add(100, 200)
    if (sum == 300) {
        return 0
    }
    return 1
}

fn fib(n) {
    if (n <= 1) {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    let res = fib(10)
    output_int(res)
    if (res == 55) {
        output("Fib(10) OK")
    } else {
        output("Fib(10) FAILED")
        return 1
    }
    return 0
}
